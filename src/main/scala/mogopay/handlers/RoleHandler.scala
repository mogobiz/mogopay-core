package mogopay.handlers

class RoleHandler {
  //  implicit object RoleInterceptor

  //  def createIfDoesntExist(roleName: RoleName) = DAO.findBy("roleName" -> roleName) map {
  //    case None => DAO.create(roleName)
  //  }
  //
  //  def findByName(roleName: String) = DAO.findBy[RoleName]("name" -> roleName)
}
